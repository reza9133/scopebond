[pytest]
markers =
    integration: hits a live GenLayer network; slow and opt-in

# Integration tests are excluded by default so `pytest` stays fast and offline.
#
#     pytest tests/integration -m integration
addopts = -m "not integration"
